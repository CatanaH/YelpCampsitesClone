Web-app for listing campsites(CRUD with REST),
Mongo DB with Mongoose,
templating with EJS,
JOI for data validation

adding:
    user authentication
    reviews posted by users


start server with 'mongod'
    (to populate server with random content, run seeds/index.js file)
run with 'node index.js'
